version: '2'
services:
    seankilgarriff.com:
        image: skilgarriff/alpine-nginx
        volumes:
            - ./seankilgarriff.com/public:/var/www/html:ro
        networks:
            - frontend
networks:
    frontend:
